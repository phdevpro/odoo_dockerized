[options]
# Paths
addons_path = /usr/lib/python3/dist-packages/odoo/addons,/mnt/extra-addons
data_dir = /var/lib/odoo

# Network
xmlrpc_port = 8069
proxy_mode = True

# Optional admin password (uncomment and set as needed)
# admin_passwd = admin

# Workers (0 = single-threaded; adjust in production)
workers = 0